<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Website</title>
    <link rel="stylesheet" href="style.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            background-color: #f4f4f4;
            color: #333;
        }

        header {
            background: #003366;
            color: #fff;
            padding: 20px 0;
            text-align: center;
        }

        nav {
            background: #0055a5;
            text-align: center;
        }

        nav a {
            color: #fff;
            padding: 14px 20px;
            display: inline-block;
            text-decoration: none;
            transition: background 0.3s;
        }

        nav a:hover {
            background: #003f7f;
        }

        .hero {
            background: url('https://source.unsplash.com/1600x500/?technology,office') no-repeat center center/cover;
            height: 500px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-size: 2.5rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.7);
        }

        .container {
            max-width: 1100px;
            margin: auto;
            padding: 20px;
        }

        .section {
            margin-bottom: 40px;
        }

        /* Comment Box */
        .comment-box {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            margin-bottom: 40px;
        }

        .comment-box h3 {
            margin-bottom: 15px;
            color: #003366;
        }

        .comment-box input, .comment-box textarea, .comment-box button {
            width: 100%;
            padding: 10px;
            margin: 6px 0;
            border-radius: 6px;
            border: 1px solid #ccc;
        }

        .comment-box button {
            background: #003366;
            color: #fff;
            cursor: pointer;
            transition: 0.3s;
        }

        .comment-box button:hover {
            background: #0055a5;
        }

        .comment-display {
            margin-top: 15px;
            background: #f9f9f9;
            padding: 10px;
            border-radius: 6px;
        }

        /* Chat Bot */
        .chat-bot-icon {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: #0055a5;
            color: #fff;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            font-size: 30px;
            text-align: center;
            line-height: 60px;
            cursor: pointer;
            box-shadow: 0 0 10px rgba(0,0,0,0.3);
            transition: transform 0.2s ease;
        }

        .chat-bot-icon:hover {
            transform: scale(1.1);
        }

        .chat-box {
            position: fixed;
            bottom: 90px;
            right: 20px;
            width: 300px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.3);
            display: none;
            flex-direction: column;
            overflow: hidden;
        }

        .chat-header {
            background: #003366;
            color: #fff;
            padding: 10px;
            text-align: center;
            font-weight: bold;
        }

        .chat-body {
            height: 250px;
            padding: 10px;
            overflow-y: auto;
            font-size: 0.9rem;
        }

        .chat-body div {
            margin: 5px 0;
        }

        .chat-input {
            display: flex;
            border-top: 1px solid #ccc;
        }

        .chat-input input {
            flex: 1;
            padding: 10px;
            border: none;
        }

        .chat-input button {
            padding: 10px;
            background: #0055a5;
            color: #fff;
            border: none;
            cursor: pointer;
        }

        footer {
            background: #003366;
            color: #fff;
            text-align: center;
            padding: 20px 0;
        }
    </style>
</head>
<body>

    <header>
        <h1>a and a music</h1>
        <p>Building trust through music</p>
    </header>

    <nav>
        <a href="#">Home</a>
        <a href="#about">About</a>
        <a href="#services">Services</a>
        <a href="#contact">Contact</a>
    </nav>

    <div class="hero">
        Welcome to Our Company
    </div>

    <div class="container">
        <section id="about" class="section">
            <h2>About Us</h2>
            <p>We are a professional group to provide help in design, development, and strategy. Our team is committed to delivering results and building long-term client relationships.</p>
        </section>

        <section id="services" class="section">
            <h2>Our Services</h2>
            <ul>
                <li>Website Design & Development</li>
                <li>Digital Marketing</li>
                <li>Logo Creation</li>
                <li>Music Creation</li>
            </ul>
        </section>

        <section id="contact" class="section">
            <h2>Contact Us to Join This Project</h2>
            <p>Email: albinbinu170@gmail.com</p>
            <p>Phone: +91 70127 87615</p>
        </section>

        <!-- Comment Box -->
        <div class="comment-box">
            <h3>Leave a Comment</h3>
            <input type="text" id="name" placeholder="Your Name">
            <textarea id="comment" rows="4" placeholder="Your Comment"></textarea>
            <button onclick="addComment()">Submit</button>
            <div id="comment-section" class="comment-display"></div>
        </div>
    </div>

    <footer>
        <p>&copy; 2025 A and A Music. All rights reserved.</p>
    </footer>

    <!-- Chatbot -->
    <div class="chat-bot-icon" onclick="toggleChat()">💬</div>

    <div class="chat-box" id="chatBox">
        <div class="chat-header">A and A Assistant</div>
        <div class="chat-body" id="chatBody"></div>
        <div class="chat-input">
            <input type="text" id="userInput" placeholder="Type your message...">
            <button onclick="sendMessage()">Send</button>
        </div>
    </div>

    <script>
        // Comment Box Logic
        function addComment() {
            const name = document.getElementById('name').value.trim();
            const comment = document.getElementById('comment').value.trim();
            const section = document.getElementById('comment-section');

            if (name && comment) {
                const div = document.createElement('div');
                div.innerHTML = `<strong>${name}</strong>: ${comment}`;
                section.appendChild(div);
                document.getElementById('name').value = '';
                document.getElementById('comment').value = '';
            } else {
                alert('Please enter your name and comment.');
            }
        }

        // Chatbot Logic
        function toggleChat() {
            const chatBox = document.getElementById('chatBox');
            chatBox.style.display = chatBox.style.display === 'flex' ? 'none' : 'flex';
        }

        function sendMessage() {
            const input = document.getElementById('userInput');
            const msg = input.value.trim();
            if (!msg) return;

            const chatBody = document.getElementById('chatBody');
            const userMsg = document.createElement('div');
            userMsg.textContent = "You: " + msg;
            chatBody.appendChild(userMsg);

            const botMsg = document.createElement('div');
            botMsg.textContent = "Bot: " + getBotReply(msg);
            chatBody.appendChild(botMsg);

            input.value = '';
            chatBody.scrollTop = chatBody.scrollHeight;
        }

        function getBotReply(msg) {
            msg = msg.toLowerCase();
            if (msg.includes("hello") || msg.includes("hi")) return "Hello! Welcome to A and A Music 🎶 How can I help you today?";
            if (msg.includes("music")) return "We create original music and background scores! Would you like to join or collaborate?";
            if (msg.includes("services") || msg.includes("what do you do")) return "We offer web design, logo creation, digital marketing, and music production.";
            if (msg.includes("join") || msg.includes("project")) return "You can join by contacting us via email: albinbinu170@gmail.com 💌";
            if (msg.includes("contact")) return "You can reach us at +91 70127 87615 or email albinbinu170@gmail.com 📞";
            if (msg.includes("bye")) return "Goodbye! Have a musical day! 🎵";
            return "Thanks for your message! We'll get back to you soon.";
        }
    </script>
</body>
</html>
